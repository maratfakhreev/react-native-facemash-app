let StyleSheet = require('react-native').StyleSheet;

module.exports = StyleSheet.create({
  header: {
    height: 65,
    backgroundColor: '#0F85FB',
    paddingTop: 20,
    alignItems: 'center',
    justifyContent: 'center'
  },
  headerText: {
    color: '#fff',
    fontSize: 18
  }
});
